name: Docker Image CI

on:
  push:
#    branches:
#      - master
#  pull_request:
#    branches:
#      - master
#  release:
#    types:
#      - published

env:
  TEST_TAG: drvdijk/docker-rclone-sync:test
  LATEST_TAG: drvdijk/docker-rclone-sync:latest

jobs:
  build:
    name: Lint, build, and push Docker image to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Lint
        uses: hadolint/hadolint-action@v3.1.0
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and export to Docker
        uses: docker/build-push-action@v5
        with:
          context: .
          load: true
          tags: ${{ env.TEST_TAG }}

      - name: Test
        run: |
          docker run --rm ${{ env.TEST_TAG }}

#      - name: Build and push
#        uses: docker/build-push-action@v5
#        with:
#          context: .
#          platforms: linux/amd64,linux/arm64
#          push: true
#          tags: ${{ env.LATEST_TAG }}
